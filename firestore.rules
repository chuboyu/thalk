rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // No client SDK access at all; only the functions (Admin SDK) touch Firestore.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
